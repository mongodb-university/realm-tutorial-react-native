import Realm from "realm";


// Invokes the shared instance of the Realm app.
// TODO: Create a Realm App instance with your Realm app ID.
// Invokes the shared instance of the Realm app.
const app = new Realm.App({id: "tasktracker-vmukh"}); // Set Realm app ID here.





export default app;

